#include<stdio.h>
int main(){
	int t,ans,n,m;
	scanf("%d",&t);
	while(t--){
		ans=0;
		scanf("%d%d",&n,&m);
		if(n==1)
			ans=0;
		else if(n==2)
			 ans=m;
		 else if(m==1)
			ans=(n-1);

		else
			ans=((n-1)+(2*(m-1)));
	printf("%d\n",ans);
	}
	return 0;
}
