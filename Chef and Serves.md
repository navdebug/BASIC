# BASIC
CodeChef Programs at beginner and easy level
for t in range (int(input())):
    l,m,K=map(int,input().split())
    if ((l+m)//K)%2==0:
        print("CHEF")
    else:
        print("COOK")
        
  
